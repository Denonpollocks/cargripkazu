<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background-color: #B8860B; color: white; padding: 20px; text-align: center; }
    .content { padding: 20px; }
    .details { background-color: #f9f9f9; padding: 15px; margin: 15px 0; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>New Quotation Request</h1>
    </div>
    <div class="content">
      <p>A new quotation request has been received.</p>
      
      <div class="details">
        <h2>User Information:</h2>
        <p>Name: {{userName}}</p>
        <p>Email: {{userEmail}}</p>
        <p>Quotation ID: {{quotationId}}</p>
        <p>Type: {{quotationType}}</p>
      </div>

      <div class="details">
        <h2>Request Details:</h2>
        {{#each details}}
          <p><strong>{{@key}}:</strong> {{this}}</p>
        {{/each}}
      </div>

      <p>Please review and respond to this request through the admin panel.</p>
    </div>
  </div>
</body>
</html> 